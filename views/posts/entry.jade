.row.entry

  .avatar-info
    // avatar
    .avatar
      a.avatar(href="#", style="background-image:url('#{avatar(user.md5, 64)}')", title="Jason's blog")
      // show this on hover, click to pop menu
      .user_info

    // the popup menu
    .user-menu
      .clearfix
        a 5123
      .clearfix
        a(href="#") Chat
      .clearfix
        a(href="#") Unfollow

  .post

    .post-controls
      // scores
      // show-comment       1
      // add comment        2
      // bookmark(favorit)  3
      // reblog             4  need comment
      // junk              -5
      // searching keywords also effect to user taste

      a(href="#") reblog&nbsp;&nbsp;
      // &#9733; for selected
      a.special-chars(href="#") &#9734;&nbsp;
      a.special-chars(href="#") &hearts;&nbsp;
      a(href="#") junk&nbsp;&#9660;
      // like has no effect, user wont click it
      // use bookmark, favorit

    .post-info
      a(href="#")= entry.author.fullname
        span :

    .post-content
      // h3 The blog title
      // .inside-content
      != entry.html

    .post-controls.right
      a.show-comments(href="javascript:void(0)") 10 comments&nbsp;&nbsp;

    .post-info
      // TODO fix time update problem
      span(rel="twipsy", date="#{entry.createTime.getTime()}", title="#{entry.createTime.format('yyyy-mm-dd HH:MM:ss')}")= smartDate(entry.createTime)

    .loading Loading...
    .comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
      .comment
        p show something comments
